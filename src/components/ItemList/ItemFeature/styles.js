import {makeStyles} from '@theme';

export const useStyles = makeStyles()(({}) => ({
  text: {
    fontWeight: 'bold',
    fontSize: 18,
  },
  clock: {
    fontWeight: 'bold',
    fontSize: 20,
  },
  clock1: {
    fontSize: 20,
  },
}));
