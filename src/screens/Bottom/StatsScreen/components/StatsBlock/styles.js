import {makeStyles} from '@theme';

export const useStyles = makeStyles()(({}) => ({
  text: {
    fontWeight: 'bold',
    fontSize: 18,
  },
  clock: {
    fontWeight: 'bold',
    fontSize: 34,
  },
  clock1: {
    fontSize: 24,
  },
  body: {
    position: 'absolute',
    top: 10,
  },
}));
