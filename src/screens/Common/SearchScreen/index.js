import {Block, Text, Header} from '@components';
import React from 'react';

const SearchScreen = () => {
  return (
    <Block flex>
      <Header title="Search" />
    </Block>
  );
};

export default SearchScreen;
