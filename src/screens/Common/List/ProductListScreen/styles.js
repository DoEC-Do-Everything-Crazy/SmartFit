import {makeStyles} from '@theme';

export const useStyles = makeStyles()(({}) => ({
  item: {
    width: 200,
    height: 260,
  },
  imageContainer: {
    backgroundColor: 'white',
    borderRadius: 8,
  },
  image: {
    width: 200,
    height: 260,
  },
}));
